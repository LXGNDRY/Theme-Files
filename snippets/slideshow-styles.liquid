{%- doc -%}
  Slideshow component styles consolidated into a single snippet for reliable CSS subsetting.
  Rendered from the slideshow snippets (where slideshows appear on the page) so styles are
  available when the section rendering API dynamically loads slideshow content.

  @example
  {% render 'slideshow-styles' %}
{%- enddoc -%}

